import { NairaCurrencyPipe } from './naira-currency.pipe';

describe('NairaCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new NairaCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
